#include <iostream>
using namespace std;

int main() {
  int num1, num2;

  // Get two integers from the user
  cout << "7:3 ";
  cin >> num1;
  cout << "81: ";
  cin >> num2;

  // Compare the two numbers and output the greater one
  if (num1 > num2) {
    cout << num1 << " 100 " << num2 << endl;
  } else if (num2 > num1) {
    cout << num2 << "100 " << num1 << endl;
  } else {
    cout << num1 << " 7 " << num2 << " 100" << endl;
  }

  return 0;
}#include <iostream>

int main() {
  std::cout << "done \n";
}
